import pandas as pd

# # 方式1:
# s = pd.Series([1, 2, 3, 4, 5], index=['a', 'b', 'c', 'd', 'e'], name='score')
# print(s, type(s))
# # s对象的values值本身就是ndarray一维数组
# print(s.values, type(s.values))
# # 方式2:字典创建
# s = pd.Series({'a': 1, 'b': 2, 'c': 3, 'd': 4, 'e': 5}, name='score')
# print(s, type(s))
#
# # 方式1:
# df = pd.DataFrame([['张三', 18, '男'], ['李四', 28, '女']], index=['a', 'b'], columns=['name', 'age', 'sex'])
# print(df, type(df))
# # s对象的values值本身就是ndarray一维数组
# print(df.values, type(df.values))
# # 方式2:字典创建
# s = pd.DataFrame({'a': 1, 'b': 2, 'c': 3, 'd': 4, 'e': 5}, name='score')
# print(df, type(df))
print('===============================================================')

# 1. 使用列表 [10, 20, 30, 40, 50] 创建Series，索引为 ['a', 'b', 'c', 'd', 'e']
s = pd.Series([10, 20, 30, 40, 50], index=['a', 'b', 'c', 'd', 'e'], name='grades')
print(s, type(s))
# 2. 给这个Series起名为 'grades'
s = s.rename('grades')
# 3. 打印Series的值和类型
print(s, type(s))
# 4. 获取Series的values属性，查看其类型
print(s, type(s.values))
print('===============================================================')
# 1. 使用字典 {'数学': 95, '语文': 88, '英语': 92, '物理': 85} 创建Series
s1 = pd.Series({'数学': 95, '语文': 88, '英语': 92, '物理': 85}, name='scores')

# 2. 给这个Series起名为 'scores'
print(s1.rename('scores'))
# 3. 打印这个Series
print(s1, type(s1))
print('===============================================================')

# 1. 使用以下数据创建DataFrame：
#    [['张三', 18, '男'], ['李四', 20, '女'], ['王五', 19, '男']]
#    索引为 ['stu1', 'stu2', 'stu3']
#    列名为 ['name', 'age', 'gender']
s2 = pd.DataFrame([['张三', 18, '男'], ['李四', 20, '女'], ['王五', 19, '男']], index=['stu1', 'stu2', 'stu3'],
                  columns=['name', 'age', 'gender'])
# 2. 打印DataFrame及其类型
print(s2, type(s2))
# 3. 获取DataFrame的values属性，查看其类型
print(s2, type(s2.values))
print('===============================================================')
# 1. 使用以下字典创建DataFrame：
#    {'name': ['张三', '李四', '王五'],
#     'age': [18, 20, 19],
#     'gender': ['男', '女', '男']}
s3 = pd.DataFrame({'name': ['张三', '李四', '王五'],
                   'age': [18, 20, 19],
                   'gender': ['男', '女', '男']}, index=['stu1', 'stu2', 'stu3'])
# 2. 打印这个DataFrame
print(s3, type(s3))
print('===============================================================')

# 1. 读取 'emp.txt' 文件（以逗号为分隔符）
#    列名为 ['name', 'age', 'dept', 'sal']
s4 = pd.read_csv('emp.txt', names=['name', 'age', 'dept', 'sal'], sep=',')
# 2. 打印读取后的DataFrame
print(f's4的数据为：{s4}', type(s4))
# 3. 查看前3行数据
print(s4.head(3))
# 4. 查看后2行数据
print(s4.tail(2))
# 5. 查看DataFrame的基本信息（行列数、数据类型等）
print(s4.info())
print('===============================================================')
# 1. 如果文件是制表符分隔的（sep='\t'），如何读取？
# s5=pd.read_csv('emp.txt', names=['name', 'age', 'dept', 'sal'], sep='\t')
# 2. 如果文件没有表头（header=None），如何读取？
s6 = pd.read_csv('emp.txt', names=['name', 'age', 'dept', 'sal'], header=None)
# 3. 如何只读取前10行数据？
print(s4.head(10))
print('===============================================================')
# 1. 使用loc获取索引为'stu1'的行

# 2. 使用loc获取列名为'name'的列

# 3. 使用loc获取'stu1'行'name'列的元素

# 4. 使用loc获取'stu1'到'stu2'行（左闭右闭）

# 5. 使用loc获取所有行，'name'和'age'列

print('===============================================================')
# 1. 在上面的df中，df.iloc[0] 和 df.loc['stu1'] 的结果是否相同？

# 2. 如果使用切片，iloc[0:2] 和 loc['stu1':'stu2'] 有什么区别？

print('===============================================================')

df = pd.read_csv('emp.txt', names=['name', 'age', 'dept', 'sal'], sep=',')

# 1. 查询所有员工的姓名和薪资

# 2. 查询薪资大于5000的员工信息

# 3. 查询技术部的员工信息

# 4. 查询薪资大于5000且部门是技术部的员工信息

# 5. 查询部门是技术部或销售部的员工信息
print('==============================================================')
# 1. 按薪资升序排列

# 2. 按薪资降序排列

# 3. 先按部门升序，部门相同时按年龄降序排列

# 4. 按年龄升序排列，只查看前3名

print('==============================================================')
df = pd.read_csv('emp.txt', names=['name', 'age', 'dept', 'sal'], sep=',')

# 1. 计算所有员工的总人数

# 2. 计算所有员工的平均薪资

# 3. 计算最高薪资和最低薪资

# 4. 计算薪资的总和
print('==============================================================')
# df = pd.DataFrame({'A': [1, 2, np.nan, 4],
#                    'B': [5, np.nan, np.nan, 8],
#                    'C': [9, 10, 11, 12]})

# 1. 统计每列的缺失值数量

# 2. 统计整个DataFrame的缺失值总数

# 3. 判断哪些位置是缺失值（返回布尔值）

print('==============================================================')
# 1. 删除包含缺失值的行

# 2. 删除包含缺失值的列

# 3. 用0填充所有缺失值

# 4. 用每列的平均值填充缺失值

# 5. 用前一个值填充缺失值（向前填充）

# 6. 用后一个值填充缺失值（向后填充）

print('==============================================================')
import pandas as pd
import numpy as np

# 综合题：员工数据分析
# 1. 读取emp.txt文件创建DataFrame
# 2. 查看基本信息（前5行、数据类型、描述性统计）
# 3. 查询技术部薪资高于5500的员工
# 4. 按部门分组统计平均薪资
# 5. 如果有缺失值，用部门平均薪资填充
# 6. 按薪资降序排列，找出薪资最高的3个员工

print('==============================================================')
